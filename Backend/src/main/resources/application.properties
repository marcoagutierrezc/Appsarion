# Configuraci?n de la base de datos (modo por defecto: local)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/epidb
spring.datasource.username=root
spring.datasource.password=123456789

# Configuraci?n de JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.eclipselink.logging.level=FINE
spring.jpa.properties.eclipselink.weaving=static

# Optimizaciones de Hibernate para mejor rendimiento
spring.jpa.properties.hibernate.jdbc.batch_size=50
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true
spring.jpa.properties.hibernate.generate_statistics=false


# Pool de conexiones HikariCP (optimizado para rendimiento)
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

#Ruta de la imagenes de supportingDocument
file.upload-dir=C:/uploads

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USER:appsarion.unillanos2@gmail.com}
spring.mail.password=${MAIL_PASS:tu-app-password-de-16-caracteres}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Configuraci�n de Backblaze B2
backblaze.keyId=${BACKBLAZE_KEY_ID}
backblaze.apiKey=${BACKBLAZE_API_KEY}
backblaze.endpoint=https://s3.us-east-005.backblazeb2.com
backblaze.region=us-east-005
backblaze.bucket=AppsarionDocumentos

spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}

# Configuraci�n de Password Reset
app.password-reset.url=${PASSWORD_RESET_URL:http://localhost:8080/reset-password}
app.password-reset.expiration-minutes=60
